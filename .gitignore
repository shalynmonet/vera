# Python
.venv/
__pycache__/
*.pyc

# Local tool config (not relevant to other clones)
.claude/

# Real personal data - never commit. Copy mock_residents.example.json to
# mock_residents.json to get started; call_log.json is regenerated by
# seed_call_log.py or by real calls through tools.py.
mock_residents.json
call_log.json

# Build artifacts / regeneratable
aws/vera-function.zip
aws/lambda-invoke-output.json

# Contains real phone numbers/transcripts from manual local testing
*.output.json
